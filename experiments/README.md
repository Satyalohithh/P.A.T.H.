# Experiments

Scratch area for research experiments. Nothing here is versioned as a product;
promising results graduate to `src/safewatch/` and `configs/`.

## Phase map

- `phase1/` — tabular baseline experiments (XGBoost on window aggregates).
- `phase2/` — temporal sequence models and cross-domain tests.